apiVersion: v1
kind: Service
metadata:
  name: &PROJECT "game-menu"
  labels:
    release: "development"
    app: *PROJECT
spec:
  type: ClusterIP
  selector:
    app: *PROJECT
  ports:
  - name: http
    port: 80
    targetPort: app
